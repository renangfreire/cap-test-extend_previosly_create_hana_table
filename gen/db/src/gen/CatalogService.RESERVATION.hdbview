VIEW CatalogService_RESERVATION AS SELECT
  RESERVATION_0.rno,
  RESERVATION_0.cno,
  RESERVATION_0.hno,
  RESERVATION_0.type,
  RESERVATION_0.arrival,
  RESERVATION_0.departure
FROM hana_db_hotel_db_HOTEL_RESERVATION AS RESERVATION_0